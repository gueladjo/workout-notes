# Screens and behaviour

The UI follows the structure of FitNotes' help pages (Home Screen, Workout Tracking, Exercises,
Progress Tracking, Routines, Calendar, Body Tracker, Settings) and its look: black app bars,
tab strips and date bar with white text, light-blue accent rules (tab indicator, Track field
labels, History day headers, the rule under each exercise name on Home), flat white cards on a
light grey ground (thin separators between sets on the Track tab only), green Save / blue Clear buttons, app logo at
the left of every app bar. Colours are tokens in `src/ui/styles.css` (`--color-bar`,
`--color-accent`, `--color-primary`, `--color-success`); the dark theme keeps the same bars and
accents over dark surfaces.

## Screen map

| Route (hash)                                                           | Screen                         | FitNotes equivalent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------------- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/`, `/workout/:date`                                                  | `Home.tsx`                     | Home Screen: app name in the bar, calendar / add / overflow, previous-next bar (swipe too; a swipe that starts in a dialog or the overflow menu is ignored, and a dialog closes if the date moves on under it) showing the weekday over "Today · date · counts" (tap = today), exercise cards with sets as number/unit columns, set limit and group bars, workout comment, empty state with Start New Workout and Copy Previous Workout (both Copy actions open the Calendar in copy mode first, as FitNotes does). A quiet backup reminder strip (Back up / Not now) appears above the cards when there is data and the last backup is over two weeks old (`src/backup/reminder.ts`). Overflow: Copy, Move, Comment, Share Workout; Routines, Body Tracker, Settings. Press-and-hold a card for selection mode (delete, move up/down, select all).                                                                                                                        |
| `/exercises`, `/exercises/:categoryId`                                 | `ExerciseList.tsx`             | Exercise List: category list (Favorites first when any), search box (every term must match), "All Exercises / routines" dropdown in the title, + to add, per-exercise menu (Edit, Favorite, History, Delete), list menu (reorder, alphabetical, colours; workout count / last used details).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `/exercise/new`, `/exercise/:id/edit`                                  | `ExerciseEditor.tsx`           | New/Edit Exercise: name, notes, category (+ new category dialog with colour palette), type, weight unit; "save and new"; unit change asks convert vs keep.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `/exercise/:id/notes`                                                  | `ExerciseNotes.tsx`            | Exercise Notes: notes with links, weight increment, default graph.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `/train/:date/:exerciseId`                                             | `Training.tsx`                 | Training Screen: Track / History / Graph tabs; nav panel drawer (exercises with set counts, Add Exercise, Add To Group, Home); trophy (records), info (notes), overflow (1RM calculator, edit exercise, home).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Track tab                                                              | `Training.tsx` (`TrackTab`)    | Fields per exercise type stacked vertically with the label above (FitNotes layout): weight and reps as a centred, fixed-width -/value/+ row (exercise increment or default), distance as the value with its unit selector beside it (metres unless pre-filled from a set), time as hh / mm / ss boxes on the numeric keypad (`DurationInputs`, also used by the goal editor and the copy/Log All set editor); Save/Clear or Update/Delete when a set is selected; press-and-hold a set and drag it to reorder (`useDragReorder`), or move it up/down with the buttons; set rows as number/unit columns (`SetValues`) with comment icon and a trophy on record sets that opens that rep count's record history on the Records screen; checkbox when Mark Sets Complete is on, with next-exercise prompt; auto-jump inside supersets; pre-fill from last workout (a pre-filled weight or distance left untouched saves the stored value back exactly, not its rounded text). |
| History tab                                                            | `HistoryTab.tsx`               | Sets grouped by date under uppercase day headers, shown as number/unit columns (`SetValues`); tap date for volume/reps (or distance/time) plus View Workout, Edit Sets, Copy Sets; tap set for est. 1RM/volume (or speed/pace, per km or per mile whatever unit the set was logged in) plus Edit Set, Copy Set. Edit/Copy actions are only offered on the Training screen (`readOnly` hides them in Exercise Overview).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Graph tab                                                              | `GraphTab.tsx`                 | Graph type dropdown (types depend on exercise type), rep-count picker for Weight and Reps / Rep Maxes, period chips and custom range, full-height chart with area fill and small points, y-axis labelled per graph type (m:ss for time and pace, whole reps, decimals as the tick spacing needs), tap point for details with previous/next, options: points, trend line, y from 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `/exercise/:id/records`                                                | `Records.tsx`                  | Records (Actual with superseded rows faded and record history per rep count, opened directly by the trophy on a set row via `?reps=N`; Estimated with rep limit setting), Stats (period: workout/week/month/year/all/custom; Workout shows and computes the viewed date when the exercise was trained that day, else its latest workout), Goals (progress bars, add/edit/delete; the editor refuses a missing or malformed target with a toast, as the Track tab refuses a set, instead of saving 0).                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `/exercise/:id/overview`                                               | `ExerciseOverview.tsx`         | Exercise Overview: History, Graph, Records, Stats, Goals in one screen (opened from workout popups). History is read-only there: quick stats and View Workout only, no Edit/Copy, because `?date=` is the day the popup was opened from, not a workout being tracked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `/calendar`, `/calendar?copy=1`                                        | `Calendar.tsx`                 | Month view as FitNotes' vertically scrolling list of months (`MonthList`, grows in both directions while scrolling; Today and previous/next scroll to the month) with category dots, workout popup (FitNotes' date dialog: light-blue date over an accent rule, uppercase exercise names with a rule, sets as right-aligned number/unit columns, Cancel / Go To), navigation bar (previous/next workout, count); List view with dots / names / sets options; drawer with Month/List and category filter (match all/any) and exercise filter (min weight/reps). With `?copy=1` (Copy Workout / Copy Previous Workout) a "Select the workout you would like to copy" banner shows and tapping a workout opens the set-selection dialog; the chosen sets are copied to the `date` in the URL.                                                                                                                                                                                 |
| `/routines`, `/routine/:id`, `/routine/new`, `/routine/:id/edit`       | `Routines.tsx`                 | Routine list; routine view with routine dropdown, days with Log All (selection dialog with edit; an exercise imported with populate type "copy previous workout" offers every set of its last workout); editor: create days, add exercises (via exercise list), predefined sets (blank = copy previous; the dialog right after adding an exercise offers Skip, otherwise Cancel, neither writes; saving without rows keeps an imported 'copy previous workout' exercise as is), groups, rename/delete/reorder days; copy/delete routine.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `/body`, `/body/measurements`, `/body/measurement/:id`                 | `BodyTracker.tsx`              | Track (latest value, delta coloured by goal; the record dialog saves an untouched pre-filled value back exactly, not its 3-decimal text), History (per day, filter), Graph (goal line); measurements list with enable/reorder; editor with unit (custom units; switching kg/lbs or cm/in asks whether to convert recorded values), goal (target value labelled with the selected unit and saved as typed, a specific goal without a positive target is refused; an untouched target converts with the values), reset/delete (rollback snapshot taken first).                                                                                                                                                                                                                                                                                                                                                                                                               |
| `/settings`, `/settings/database`                                      | `Settings.tsx`, `Database.tsx` | General, Home Screen, Training toggles; Data: Save/Share Backup, Restore, rollback snapshots, CSV exports, Calculate Personal Records, Delete Workout History, Database diagnostics, storage status.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| (no route; while another instance hands over, or after it took over)   | `Instance.tsx`                 | Waiting: spinner while the other tab/window/app saves and stops. Taken over: "WorkoutNotes is open in another window" with a "Use WorkoutNotes here" button (reloads, which takes the database back) and, when the last save failed, a button to save a copy of this window's data as a backup file. See `src/app/instance.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| (no route; replaces the app when the stored database cannot be opened) | `Recovery.tsx`                 | Recovery: shows the open error and the size of the stored bytes, offers to save the unreadable file, restore any rollback snapshot, or start with an empty database. Every action keeps the unreadable bytes as a snapshot first (`src/app/recovery.ts`), then the app boots again. Another window opened meanwhile takes over as from the running app; this one then shows the taken-over screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

## Conventions

- `Dialog` (`src/ui/components/Dialog.tsx`) reports `onClose` only for closes the user made
  (Escape, backdrop, the buttons a screen wires to it). Setting `open` to false closes it silently,
  so a screen can swap two dialogs on one piece of state (`GroupDialog`, `DeleteHistoryDialog`).
- A dialog whose fields are `useState` initialised from props is mounted only while open
  (`{open && <XDialog open … />}`, as `GroupDialog`, `ExerciseFilterDialog`, `UnitDialog` and
  `DeleteHistoryDialog` are), or resets its fields on open with the `seen` pattern (`RecordDialog`,
  `GoalEditor`); a permanently mounted dialog would keep stale text from its previous opening.

## Deviations from FitNotes

- Re-ordering exercises, categories, routine days/exercises and measurements uses up/down
  buttons instead of press-and-drag. Sets on the Track tab do support press-and-hold drag
  (`src/ui/components/useDragReorder.ts`, pointer events plus a non-passive `touchmove` listener
  so the page does not scroll mid-drag), with the up/down buttons kept as the alternative.
- The calendar's "workout panel" option is a dialog list of workouts instead of a split panel.
- Routine editing uses the exercise list with a `returnTo` parameter (see
  `src/app/return-to.ts`) instead of a dedicated Select Exercise screen.
- The 1RM calculator is available; the set and plate calculators are not (see below).

## Not implemented

Deliberately left out as superfluous for this app or as later work:

- Rest timer and workout timer (`WorkoutTime` rows are preserved and shown nowhere).
- Plate calculator and set calculator (`Plate`/`Barbell` tables preserved).
- Analysis screens (yearly graphs, rep-max grid; `RepMaxGridFavourite`/`ExerciseGraphFavourite`
  preserved).
- Automatic backups to cloud storage; "share graph as image".
